import dotenv from "dotenv";
dotenv.config();

export const PORT = process.env.PORT || 6005;

export const mongoDBUrl = {/* Place your MongoDB database URL */}

//use env variables for the password and username
